spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://${MYSQL_URL}/wechat?autoReconnect=true&useUnicode=true
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASS:root}